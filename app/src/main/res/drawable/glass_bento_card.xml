<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape>
            <corners android:radius="@dimen/roundedImageRadius" />
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <layer-list>
            <!-- Background Layer: Translucent surface -->
            <item>
                <shape>
                    <solid android:color="#1AFFFFFF" /> <!-- 10% White tint -->
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
            <!-- Specular Gradient Layer -->
            <item>
                <shape>
                    <gradient
                        android:startColor="#0DFFFFFF"
                        android:endColor="#00FFFFFF"
                        android:type="linear"
                        android:angle="135" />
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
            <!-- Frosted Border Stroke -->
            <item>
                <shape>
                    <solid android:color="@android:color/transparent" />
                    <corners android:radius="@dimen/roundedImageRadius" />
                    <stroke
                        android:width="1dp"
                        android:color="#33FFFFFF" /> <!-- 20% White border -->
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>
