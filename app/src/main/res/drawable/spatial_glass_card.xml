<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape>
            <corners android:radius="@dimen/roundedImageRadius" />
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <layer-list>
            <!-- Contrast Overlay: Darker in light modes, subtle in dark modes -->
            <item>
                <shape>
                    <solid android:color="#1A000000" />
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
            
            <!-- Glass Translucency Panel -->
            <item>
                <shape>
                    <solid android:color="?attr/floatingNavBackground" /> 
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
            
            <!-- Premium Frosted Border Stroke: High contrast for Amoled -->
            <item>
                <shape>
                    <stroke android:width="1.2dp" android:color="?attr/floatingNavStroke" /> 
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>
