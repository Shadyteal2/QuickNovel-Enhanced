<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape>
            <corners android:radius="@dimen/roundedImageRadius" />
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <layer-list>
            <!-- Thinner Glass: 30% Alpha Slate for maximum background reveal -->
            <item>
                <shape>
                    <solid android:color="@color/vibe_glass_bg" />
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
            
            <item>
                <shape>
                    <stroke android:width="1dp" android:color="@color/vibe_glass_stroke" /> 
                    <corners android:radius="@dimen/roundedImageRadius" />
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>
